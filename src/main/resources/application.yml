server:
  port: 8080
      
spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/usercrud
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    properties:
      hibernate:
        generate_statistics: true
        format_sql: true
    hibernate:
      ddl-auto: update 
logging:
  level:
    org:
      hibernate:
        stat: debug
        type: trace
    org.springframework.security: DEBUG
  jpa:
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto : update
  http:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1000MB
    encoding:
      charset: UTF-8
      enabled: true
      force: true

  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/
  jackson:
    default-property-inclusion: NON_NULL
  mvc:
    contentnegotiation:  #boot2
      favor-path-extension: false
 
security:
  oauth2:
#    sso:
#      loginPath: /auth/login
    client:
      client-id: 222
      client-secret: 0XL5PKZPR42enw8Fu6HVC9cah4Ma
      access-token-uri: https://newac.herokuapp.com/sessions/get_access_token
      user-authorization-uri: https://newac.herokuapp.com/sessions/new
      clientAuthenticationScheme: form
      scope: openid,profile,email
      pre-established-redirect-uri: http://localhost:8080/auth/login
      registered-redirect-uri: http://localhost:8080/auth/loginuiuiu
      use-current-uri: false
#      token-name: Basic
    resource:
      userInfoUri: http://newac.herokuapp.com/sessions/user_info
#      token-type: Bearer
